[defaults]
inventory = inventory.ini
host_key_checking = False
remote_user = cisco
timeout = 30
interpreter_python = auto_silent
# Remote nodes may lack a writable ~/.ansible/tmp early in boot; /tmp avoids mkdir failures.
remote_tmp = /tmp/.ansible-tmp
